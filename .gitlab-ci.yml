build-job:
  image: docker:latest
  services:
    -docker:dind
  stage: build
  script: 
    - unset DOCKER_HOST
    - docker compose build
    - echo "Built"

test-job: 
  stage: test
  script: 
    - echo "testing"

deploy:
  stage: deploy
  script: 
    - docker compose up 
    - echo "Deployed"
  environment: production