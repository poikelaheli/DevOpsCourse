http {
    upstream backend {
        server nodeServer:8210;
    }
    server {
        listen 80;

        location / {
            auth_basic "Restricted Access";
            auth_basic_user_file /etc/nginx/.htpasswd;
            root /home;
            index index.html;
        }
    }
}